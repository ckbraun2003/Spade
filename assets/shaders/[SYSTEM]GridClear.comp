#version 430 core

layout(local_size_x = 64) in;

layout(std430, binding = 9) buffer GridHeadData {
    int gridHead[];
};

uniform int totalCells;

void main() {
    uint index = gl_GlobalInvocationID.x;
    if (index >= totalCells) return;

    gridHead[index] = -1;
}
